# Audit: AUTH-TODO.md false/unchecked completions

**Date:** 2026-09-03
**Status:** findings only — no source changes

## Summary

5 items are marked `[ ]` despite having working code on disk.
1 checked item has a stale description that contradicts the implementation.

---

## Findings

### 1. Line 68 — session expiry/invalidation + cookie-flag tests

| Field | Value |
|-------|-------|
| Claim | "Add session expiry/invalidation and cookie-flag integration tests" |
| Checkbox | `[ ]` |
| Actual | `session.test.ts` covers expiry, single invalidation, all-sessions invalidation, inactive-user rejection. `cookies.test.ts` covers HttpOnly / SameSite / Secure-in-production / clearing. |
| Verdict | **Should be `[x]`.** Code exists and passes. |

### 2. Line 115 — email delivery for password reset

| Field | Value |
|-------|-------|
| Claim | "Deliver reset instructions through an email provider" |
| Checkbox | `[ ]` |
| Actual | `src/lib/mail.ts` implements `sendPasswordResetEmail` via nodemailer + `SMTP_*` env vars. `forgot-password/route.ts:25` calls it. Falls back to `console.info` when SMTP is unconfigured. |
| Verdict | **Should be `[x]` (SMTP-dependent).** The delivery plumbing is implemented; production needs env vars, same as line 296 already acknowledges. |

### 3. Line 123 — auth-route integration tests

| Field | Value |
|-------|-------|
| Claim | "Add auth-route integration tests" |
| Checkbox | `[ ]` |
| Actual | `auth-flows.test.ts` covers login success/failure, logout, forgot-password request, reset-password single-use, session cookie validation. |
| Verdict | **Should be `[x]`.** |

### 4. Line 139 — route-level 401/403 integration tests

| Field | Value |
|-------|-------|
| Claim | "Add consistent route-level 401 and 403 integration tests" |
| Checkbox | `[ ]` |
| Actual | `route-protection.test.ts` asserts 401 for missing sessions and 403 for wrong roles on employee routes. |
| Verdict | **Should be `[x]`.** |

### 5. Line 151 — cross-business tests

| Field | Value |
|-------|-------|
| Claim | "Add cross-business tests for employees, payroll, reports, settings, FBT, payslips, and dashboard" |
| Checkbox | `[ ]` |
| Actual | `tenant-isolation.test.ts` covers employees only (3 cases: A reads own employees, A creates in own business, SUPER_ADMIN gets 403). No cross-business tests exist for payroll, reports, settings, FBT, payslips, or dashboard. |
| Verdict | **Partially complete.** Should be split: employee cross-business → `[x]`; remaining resources → keep `[ ]` with a note. |

### 6. Line 78 — rate limiter description (stale, but correctly checked)

| Field | Value |
|-------|-------|
| Claim | "Add in-memory login rate limiter" |
| Actual | `rate-limit.ts` uses `prisma.rateLimit` (DB-backed) from the start. |
| Verdict | **Description is stale.** Should read "Add login rate limiter (DB-backed via RateLimit model)". Line 85 already has the parenthetical justification; line 78's "in-memory" text is misleading. |

---

## Recommended fixes

```diff
@@ AUTH-TODO.md

 ## Phase 2: Password, Session, Cookie, and User Utilities

 - [x] Add `src/lib/auth/password.ts`.
 ...
 - [x] Add unit tests for hashing, permission mapping, tenant access, and rate limiting.
-- [ ] Add session expiry/invalidation and cookie-flag integration tests.
+- [x] Add session expiry/invalidation and cookie-flag integration tests.
+  (`session.test.ts` — expiry, single/all invalidation, inactive rejection;
+   `cookies.test.ts` — HttpOnly, SameSite, Secure-in-production, clearing.)

 ## Phase 3: Roles, Permissions, Validation, and Rate Limiting

 ...
-- [x] Add in-memory login rate limiter.
+- [x] Add login rate limiter (DB-backed via `RateLimit` Prisma model).
  ...
  [rest of Phase 3 unchanged]

 ## Phase 4: Authentication API Routes

 ...

 ### Password reset

 ...
-- [ ] Deliver reset instructions through an email provider.
+- [x] (SMTP-dependent) Deliver reset instructions through an email provider.
+  (`src/lib/mail.ts` uses nodemailer + `SMTP_*` env vars; falls back to
+   console log when unconfigured.)
 ...
-- [ ] Add auth-route integration tests.
+- [x] Add auth-route integration tests.
+  (`auth-flows.test.ts` — login, logout, forgot-password, reset-password flows.)

 ## Phase 5: API Protection and Tenant Isolation

 ...

 ### Protected routes

 ...
-- [ ] Add consistent route-level 401 and 403 integration tests.
+- [x] Add consistent route-level 401 and 403 integration tests.
+  (`route-protection.test.ts` — 401 for missing sessions, 403 for wrong roles.)

 ### Tenant scoping

 ...
-- [ ] Add cross-business tests for employees, payroll, reports, settings, FBT, payslips, and dashboard.
+- [x] Add cross-business tests for employees.
+  (`tenant-isolation.test.ts` — Business A isolation, SUPER_ADMIN 403.)
+- [ ] Add cross-business tests for payroll, reports, settings, FBT, payslips, and dashboard.
```

---

## Validation

After applying the fixes:

1. `rg -n "\[ \]" AUTH-TODO.md` should show only:
   - Phase 1 carryovers (lines 32, 33, 40, 46, 47)
   - Phase 2 cookie-flag tests (if not fixed above)
   - Phase 5 remaining cross-business resources
   - Phase 6 browser smoke tests
   - Phase 10 transaction wrapper
   - Phase 12 (already all checked)
   - Final Acceptance item 10 (Neon-blocked)

2. `rg -n "in-memory login rate limiter" AUTH-TODO.md` → 0 results
